import * as React from 'react'
import { EmptyState, Shell } from '@/components/Shell'

// Placeholder: replaced by the slice that owns this screen (see the control plane v2 PRD).
export const CostsPage = (): React.ReactElement => <Shell title="Costs"><EmptyState>Coming in control plane v2.</EmptyState></Shell>
